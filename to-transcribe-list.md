# Ingest queue — sources to process next.
# YouTube URLs go through the wiki-ingest-next skill (transcribe -> ingest -> commit).
# Web article URLs can be clipped with the Obsidian Web Clipper or pasted directly,
# then ingested into the wiki with the karpathy-llm-wiki skill.
# Once processed, move the URL from `## pending` to `## done`.

## pending


## done

